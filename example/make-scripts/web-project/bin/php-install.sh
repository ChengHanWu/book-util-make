#!/usr/bin/env bash

THE_BASE_DIR_PATH=$(cd -P -- "$(dirname -- "$0")" && pwd -P)

sudo apt-get install php-cli

# http://packages.ubuntu.com/trusty/php5-cli
# http://packages.ubuntu.com/xenial/php-cli
